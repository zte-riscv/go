name: go vet compile
on:
  pull_request:
  workflow_dispatch:

jobs:
  go-vet-compile:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build toolchain and test (host amd64)
        run: |
            # install bootstrap Go
            set -euxo pipefail
            BOOT=/tmp/go-bootstrap
            curl -fsSL https://go.dev/dl/go1.25.0.linux-amd64.tar.gz -o /tmp/go.tar.gz
            tar -C /tmp -xzf /tmp/go.tar.gz
            mv /tmp/go "$BOOT"
            export GOROOT_BOOTSTRAP="$BOOT"

            # build toolchain
            cd "$GITHUB_WORKSPACE/src"
            ./make.bash

            # run go vet using the newly built repository toolchain
            GOROOT="$GITHUB_WORKSPACE" "$GOROOT/bin/go" vet cmd/compile/...


